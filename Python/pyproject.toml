[project]
name = "pi"
version = "0.1.0"
description = "Pi — Python AI coding agent"
requires-python = ">=3.11"
dependencies = [
    "google-genai>=1.74.0",
    "python-dotenv>=1.2.2",
]

[tool.uv]
package = true

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
